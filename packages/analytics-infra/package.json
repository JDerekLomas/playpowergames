{
  "name": "analytics-infra",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "bootstrap": "pnpm build && cdk bootstrap",
    "deploy": "pnpm build && cdk deploy"
  },
  "dependencies": {
    "aws-cdk-lib": "2.189.1",
    "constructs": "^10.3.0",
    "source-map-support": "^0.5.21"
  },
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.911.0",
    "@aws-sdk/lib-dynamodb": "^3.911.0",
    "@types/aws-lambda": "^8.10.129",
    "dotenv": "^17.2.3",
    "esbuild": "^0.25.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}